#
# $GNUPGHOME/gpg-agent.conf
#

# Note: This file must exist next to GnuPG data (set with GNUPGHOME).

# Seconds of inactivity before a cached passphrase expires (resets on each use).
default-cache-ttl 3600
# Absolute maximum a passphrase stays cached, regardless of use.
max-cache-ttl 28800
