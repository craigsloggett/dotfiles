plugin_cache_dir             = "${XDG_CACHE_HOME}/terraform"
disable_checkpoint           = true
disable_checkpoint_signature = true

provider_installation {
  dev_overrides {
    "craigsloggett/github"            = "${GOPATH}/bin"
    "craigsloggett/utility-functions" = "${GOPATH}/bin"
  }

  direct {}
}
